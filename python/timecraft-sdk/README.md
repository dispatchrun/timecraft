# timecraft python sdk

## Use

See `test_client.py`.

## Install

```
pip install . --upgrade --target deps/
```

Adjust `deps/` to wherever makes sense for your deployment.

## Test

`./test.sh` runs the test suite inside timecraft.
